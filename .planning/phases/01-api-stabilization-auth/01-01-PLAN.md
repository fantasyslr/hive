---
phase: 01-api-stabilization-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/schemas.ts
  - packages/hive-gw/src/services/task-machine.ts
  - packages/hive-gw/src/services/registry.ts
  - packages/hive-gw/src/services/dispatcher.ts
  - packages/hive-gw/src/services/p2p-proxy.ts
  - packages/hive-gw/src/services/verify-loop.ts
  - packages/hive-gw/src/services/memory-service.ts
  - packages/hive-gw/src/services/board-persistence.ts
  - packages/hive-gw/src/routes/tasks.ts
  - packages/hive-gw/src/routes/agents.ts
  - packages/hive-gw/src/routes/board.ts
  - packages/hive-gw/src/routes/events.ts
  - packages/hive-gw/src/routes/memory.ts
  - packages/hive-gw/src/routes/heartbeat.ts
  - packages/hive-gw/src/routes/feishu-webhook.ts
  - packages/hive-gw/src/routes/task-fields.test.ts
  - packages/hive-gw/src/routes/task-orchestration.test.ts
  - packages/hive-gw/src/routes/task-reject.test.ts
  - packages/hive-gw/src/routes/events.test.ts
  - packages/hive-gw/src/routes/review-fixes.test.ts
  - packages/hive-gw/src/services/task-machine.test.ts
  - packages/hive-gw/src/services/dispatcher.test.ts
  - packages/hive-gw/src/services/registry.test.ts
  - packages/hive-gw/src/services/p2p-proxy.test.ts
  - packages/hive-gw/src/services/memory-service.test.ts
  - packages/hive-gw/src/services/verify-loop.test.ts
  - packages/hive-gw/src/services/task-delete.test.ts
  - packages/hive-gw/src/services/task-release.test.ts
  - packages/hive-gw/src/middleware/error-handler.test.ts
autonomous: true
requirements:
  - API-01

must_haves:
  truths:
    - "Every field in Task interface uses camelCase (no underscores in field names)"
    - "Every Zod schema field uses camelCase"
    - "All existing tests pass with zero snake_case field references"
    - "API responses contain only camelCase field names"
  artifacts:
    - path: "packages/shared/src/types.ts"
      provides: "Task, AgentCard, RegisteredAgent, P2PRequest, P2PResponse, RoutingScore, MemoryConclusion types"
      contains: "fromAgentId"
    - path: "packages/shared/src/schemas.ts"
      provides: "All Zod validation schemas"
      contains: "agentId"
    - path: "packages/hive-gw/src/services/task-machine.ts"
      provides: "Task state machine with camelCase fields"
      contains: "retryCount"
  key_links:
    - from: "packages/shared/src/types.ts"
      to: "packages/hive-gw/src/services/task-machine.ts"
      via: "Task type import"
      pattern: "fromAgentId|toAgentId|contextRef|taskKind|parentTaskId|runId|verificationRequired|retryCount|outputRefs"
    - from: "packages/shared/src/schemas.ts"
      to: "packages/hive-gw/src/routes/tasks.ts"
      via: "Schema validation on request bodies"
      pattern: "agentId.*version"
---

<objective>
Rename all snake_case API fields to camelCase across the entire hive-gw codebase (types, schemas, services, routes, tests).

Purpose: API-01 requires consistent camelCase naming before adding new API surface. This is a pure mechanical rename — no behavior changes.
Output: All `_`-separated field names in types.ts, schemas.ts, task-machine.ts, routes, and tests become camelCase. All tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Fields to rename (snake_case -> camelCase): -->

In types.ts (Task interface):
  output_refs -> outputRefs
  from_agent_id -> fromAgentId
  to_agent_id -> toAgentId
  context_ref -> contextRef
  task_kind -> taskKind
  parent_task_id -> parentTaskId
  run_id -> runId
  verification_required -> verificationRequired
  retry_count -> retryCount

In types.ts (AgentCard):
  agent_id -> agentId

In types.ts (MemoryConclusion):
  task_id -> taskId
  agent_id -> agentId
  decision_reason -> decisionReason
  impact_scope -> impactScope

In types.ts (RoutingScore):
  agent_id -> agentId

In types.ts (P2PRequest):
  from_agent_id -> fromAgentId
  timeout_ms -> timeoutMs

In types.ts (P2PResponse):
  from_agent_id -> fromAgentId
  to_agent_id -> toAgentId
  latency_ms -> latencyMs

In types.ts (FeishuChangeEvent):
  event_type -> eventType
  app_token -> appToken
  table_id -> tableId
  document_id -> documentId
  operator_id -> operatorId

In schemas.ts:
  AgentRegistrationSchema: agent_id -> agentId
  CreateTaskSchema: from_agent_id -> fromAgentId, to_agent_id -> toAgentId, context_ref -> contextRef, task_kind -> taskKind, parent_task_id -> parentTaskId, run_id -> runId, verification_required -> verificationRequired
  ClaimTaskSchema: agent_id -> agentId
  UpdateTaskSchema: agent_id -> agentId, output_refs -> outputRefs
  P2PRequestSchema: from_agent_id -> fromAgentId, timeout_ms -> timeoutMs
  PublishEventSchema: agent_id -> agentId

IMPORTANT: Do NOT rename event type string VALUES like 'task.completed', 'agent.online' etc. in constants.ts — those are event names using dot notation, not field names.
IMPORTANT: Do NOT rename agentId in URL path parameters like `req.params.agent_id` — Express route params use the URL pattern `:agent_id`. However, the route path string itself stays as `/agents/:agent_id` (URL convention), but destructured body fields must become camelCase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename snake_case fields to camelCase in shared types and schemas</name>
  <files>packages/shared/src/types.ts, packages/shared/src/schemas.ts</files>
  <read_first>
    packages/shared/src/types.ts
    packages/shared/src/schemas.ts
    packages/shared/src/constants.ts
  </read_first>
  <action>
Rename every snake_case field in types.ts and schemas.ts to camelCase per the mapping in the interfaces block above. This is a mechanical find-and-replace within these two files.

For types.ts — rename all fields listed in the interfaces block. The Task, AgentCard, RegisteredAgent, MemoryConclusion, RoutingScore, P2PRequest, P2PResponse, FeishuChangeEvent, and BoardSnapshot interfaces must have zero underscore-separated field names when done.

For schemas.ts — rename the Zod schema field names (the keys inside z.object()) to match the new camelCase type fields. Every schema field that was snake_case must become camelCase:
- AgentRegistrationSchema: `agent_id` -> `agentId`
- CreateTaskSchema: `from_agent_id` -> `fromAgentId`, `to_agent_id` -> `toAgentId`, `context_ref` -> `contextRef`, `task_kind` -> `taskKind`, `parent_task_id` -> `parentTaskId`, `run_id` -> `runId`, `verification_required` -> `verificationRequired`
- ClaimTaskSchema: `agent_id` -> `agentId`
- UpdateTaskSchema: `agent_id` -> `agentId`, `output_refs` -> `outputRefs`
- PublishEventSchema: `agent_id` -> `agentId`
- P2PRequestSchema: `from_agent_id` -> `fromAgentId`, `timeout_ms` -> `timeoutMs`

Do NOT touch constants.ts — event type string values ('task.completed' etc.) use dot notation, not underscores.
  </action>
  <verify>
    <automated>cd /Users/slr/hive && grep -rn '_' packages/shared/src/types.ts packages/shared/src/schemas.ts | grep -E '\b[a-z]+_[a-z]+\b' | grep -v '\/\/' | grep -v "import " | grep -v "'.*\..*'" | grep -v "as const" || echo "PASS: no snake_case fields found"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `agent_id` in types.ts returns 0 matches (excluding comments)
    - grep for `from_agent_id` in schemas.ts returns 0 matches
    - grep for `agentId` in types.ts returns matches in AgentCard, RoutingScore, MemoryConclusion
    - grep for `fromAgentId` in schemas.ts returns matches in CreateTaskSchema, P2PRequestSchema
    - File compiles: `cd /Users/slr/hive && npx tsc --noEmit -p packages/shared/tsconfig.json` (or equivalent) shows no type errors in these 2 files
  </acceptance_criteria>
  <done>All field names in types.ts and schemas.ts are camelCase. Zero snake_case field names remain (excluding comments).</done>
</task>

<task type="auto">
  <name>Task 2: Propagate camelCase renames to all services, routes, and tests</name>
  <files>
    packages/hive-gw/src/services/task-machine.ts
    packages/hive-gw/src/services/registry.ts
    packages/hive-gw/src/services/dispatcher.ts
    packages/hive-gw/src/services/p2p-proxy.ts
    packages/hive-gw/src/services/verify-loop.ts
    packages/hive-gw/src/services/memory-service.ts
    packages/hive-gw/src/services/board-persistence.ts
    packages/hive-gw/src/routes/tasks.ts
    packages/hive-gw/src/routes/agents.ts
    packages/hive-gw/src/routes/board.ts
    packages/hive-gw/src/routes/events.ts
    packages/hive-gw/src/routes/memory.ts
    packages/hive-gw/src/routes/heartbeat.ts
    packages/hive-gw/src/routes/feishu-webhook.ts
    packages/hive-gw/src/routes/task-fields.test.ts
    packages/hive-gw/src/routes/task-orchestration.test.ts
    packages/hive-gw/src/routes/task-reject.test.ts
    packages/hive-gw/src/routes/events.test.ts
    packages/hive-gw/src/routes/review-fixes.test.ts
    packages/hive-gw/src/services/task-machine.test.ts
    packages/hive-gw/src/services/dispatcher.test.ts
    packages/hive-gw/src/services/registry.test.ts
    packages/hive-gw/src/services/p2p-proxy.test.ts
    packages/hive-gw/src/services/memory-service.test.ts
    packages/hive-gw/src/services/verify-loop.test.ts
    packages/hive-gw/src/services/task-delete.test.ts
    packages/hive-gw/src/services/task-release.test.ts
    packages/hive-gw/src/middleware/error-handler.test.ts
  </files>
  <read_first>
    packages/hive-gw/src/services/task-machine.ts
    packages/hive-gw/src/routes/tasks.ts
    packages/hive-gw/src/routes/agents.ts
    packages/hive-gw/src/services/dispatcher.ts
    packages/hive-gw/src/services/p2p-proxy.ts
    packages/hive-gw/src/services/verify-loop.ts
    packages/hive-gw/src/services/memory-service.ts
    packages/hive-gw/src/services/board-persistence.ts
    packages/hive-gw/src/routes/events.ts
    packages/hive-gw/src/routes/memory.ts
    packages/hive-gw/src/routes/heartbeat.ts
    packages/hive-gw/src/routes/feishu-webhook.ts
  </read_first>
  <action>
Apply the same snake_case -> camelCase rename mapping across ALL service files, route files, and test files in packages/hive-gw/.

Key areas to update in each file:

**task-machine.ts:** The `create()` method parameter names and spread assignments use snake_case (e.g., `params.from_agent_id`, `retry_count: 0`). Rename to camelCase: `params.fromAgentId`, `retryCount: 0`, etc. The `retry()` method references `retry_count` — rename to `retryCount`. The `restore()` method signature has `retry_count` — rename.

**routes/tasks.ts:** Destructured body fields `{ agent_id, version }` in claim/reject/patch handlers must become `{ agentId, version }`. Event emit data objects use `task_id` and `agent_id` — rename to `taskId` and `agentId`. The `output_refs` destructure and usage must become `outputRefs`.

**routes/agents.ts:** Event emit data uses `agent_id` — rename to `agentId`. P2P handler destructures `from_agent_id`, `timeout_ms` — rename to `fromAgentId`, `timeoutMs`. The `forwardP2PRequest` call passes `from_agent_id`, `to_agent_id` — rename.

**services/dispatcher.ts:** References to `agent_id` field on AgentCard/RegisteredAgent must become `agentId`. The `scoreAgent` function and `Dispatcher` class access `.agent_id` on agent objects — rename to `.agentId`.

**services/registry.ts:** All `.agent_id` references on AgentCard must become `.agentId`. The Map key lookups use `card.agent_id` — rename to `card.agentId`.

**services/p2p-proxy.ts:** `from_agent_id`, `to_agent_id`, `timeout_ms`, `latency_ms` in the proxy function — rename to camelCase.

**services/verify-loop.ts:** Any `task_id`, `agent_id` references — rename.

**services/memory-service.ts:** Any `task_id`, `agent_id`, `output_refs` references — rename.

**services/board-persistence.ts:** Any `agent_id`, `task_id` references in snapshot logic — rename.

**All test files:** Update every test assertion and setup that references old snake_case field names to use the new camelCase names. For example in task-fields.test.ts: `from_agent_id: 'orchestrator'` -> `fromAgentId: 'orchestrator'`, `expect(parsed.data.from_agent_id)` -> `expect(parsed.data.fromAgentId)`, etc.

**Event data objects:** When events emit `{ task_id: ..., agent_id: ... }`, rename to `{ taskId: ..., agentId: ... }`. These are data payload fields, not event type names.

**Express route path params:** Keep URL param names as-is in route definitions (`:agent_id` in URL paths is a URL convention). But when accessing `req.params.agent_id`, that stays because it matches the URL pattern — this is NOT a field rename target.

Run `npm test` after all changes to verify nothing breaks.
  </action>
  <verify>
    <automated>cd /Users/slr/hive && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits with code 0 (all tests pass)
    - grep for `from_agent_id` across packages/hive-gw/src/ returns 0 matches in .ts files (excluding route URL params in path strings)
    - grep for `retry_count` in task-machine.ts returns 0 matches
    - grep for `output_refs` in routes/tasks.ts returns 0 matches (field references — `outputRefs` used instead)
    - grep for `\.agent_id` in services/dispatcher.ts returns 0 matches
    - grep for `agent_id:` (as object key) in routes/agents.ts event emits returns 0 matches — replaced by `agentId:`
  </acceptance_criteria>
  <done>Every snake_case field reference in hive-gw services, routes, and tests is renamed to camelCase. All tests pass. API-01 is complete.</done>
</task>

</tasks>

<verification>
1. `cd /Users/slr/hive && npm test` — all tests pass
2. `grep -rn 'agent_id\|from_agent_id\|to_agent_id\|task_id\|retry_count\|output_refs\|context_ref\|task_kind\|parent_task_id\|run_id\|verification_required\|timeout_ms\|latency_ms\|decision_reason\|impact_scope\|event_type\|app_token\|table_id\|document_id\|operator_id' packages/shared/src/types.ts packages/shared/src/schemas.ts` — zero matches (excluding comments and imports)
3. `grep -rn '\.agent_id\b' packages/hive-gw/src/services/ packages/hive-gw/src/routes/` — only matches are Express `req.params.agent_id` in URL path access (acceptable)
</verification>

<success_criteria>
- Zero snake_case field names in types.ts and schemas.ts
- Zero snake_case field references in service/route code (except URL path params)
- All existing tests pass with updated field names
- API-01 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-stabilization-auth/01-01-SUMMARY.md`
</output>
