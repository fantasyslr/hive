---
phase: 02-memory-enhancement
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/hive-memory/src/server.ts
  - packages/hive-memory/src/server.test.ts
  - packages/shared/src/schemas.ts
  - packages/hive-gw/src/services/memory-service.ts
  - packages/hive-gw/src/routes/memory.ts
  - packages/hive-gw/src/services/memory-service.test.ts
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03, MEM-04, MEM-05]

must_haves:
  truths:
    - "memory_add MCP tool accepts namespace, agentId, taskId, ttlMs parameters"
    - "memory_search MCP tool accepts namespace, agentId, after, before filter parameters"
    - "Gateway MemoryService passes agentId, taskId, namespace as dedicated fields on writes"
    - "Gateway search route accepts namespace, agentId, after, before query params"
    - "Existing MCP tool calls without new params still work (backward compatible)"
    - "Search results include namespace, agentId, taskId, expiresAt in returned records"
  artifacts:
    - path: "packages/hive-memory/src/server.ts"
      provides: "MCP tools with extended input schemas for namespace, source metadata, TTL, filters"
      contains: "namespace"
    - path: "packages/hive-memory/src/server.test.ts"
      provides: "MCP integration tests for new parameters"
      min_lines: 80
    - path: "packages/shared/src/schemas.ts"
      provides: "Updated MemorySearchSchema with agentId, after, before fields"
      contains: "agentId"
    - path: "packages/hive-gw/src/services/memory-service.ts"
      provides: "Gateway memory service using namespace field directly"
      contains: "namespace"
    - path: "packages/hive-gw/src/routes/memory.ts"
      provides: "Search route accepting filter query params"
      contains: "agentId"
    - path: "packages/hive-gw/src/services/memory-service.test.ts"
      provides: "Updated tests for new search/write behavior"
      contains: "namespace"
  key_links:
    - from: "packages/hive-memory/src/server.ts"
      to: "packages/hive-memory/src/store.ts"
      via: "store.add/search with new params"
      pattern: "store\\.(add|search)"
    - from: "packages/hive-gw/src/services/memory-service.ts"
      to: "packages/hive-gw/src/services/memory-client.ts"
      via: "callTool with namespace/agentId/taskId args"
      pattern: "callTool"
    - from: "packages/hive-gw/src/routes/memory.ts"
      to: "packages/hive-gw/src/services/memory-service.ts"
      via: "Route passes filter params to service.search"
      pattern: "memoryService\\.search"
---

<objective>
Wire the enhanced store capabilities (from Plan 01) through the MCP server tools, shared schemas, Gateway MemoryService, and search route — so agents can use namespace, source tracking, TTL, dedup, and filtered search via both MCP protocol and HTTP API.

Purpose: Plan 01 adds store primitives. Without wiring them through MCP tools and Gateway, the features are invisible to agents.
Output: Updated MCP tools, shared MemorySearchSchema, Gateway MemoryService, and search route — all passing through new parameters end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-enhancement/02-01-SUMMARY.md
@packages/hive-memory/src/server.ts
@packages/hive-memory/src/store.ts
@packages/shared/src/schemas.ts
@packages/shared/src/constants.ts
@packages/hive-gw/src/services/memory-service.ts
@packages/hive-gw/src/services/memory-client.ts
@packages/hive-gw/src/routes/memory.ts
@packages/hive-gw/src/services/memory-service.test.ts

<interfaces>
<!-- From Plan 01 — store.ts will have these after execution -->

Expected store.add() signature after Plan 01:
```typescript
add(params: {
  title?: string;
  content: string;
  metadata?: Record<string, unknown>;
  namespace?: string;      // NEW — dedicated field, default ''
  agentId?: string;        // NEW — source tracking
  taskId?: string;         // NEW — source tracking
  ttlMs?: number;          // NEW — auto-expire
}): MemoryRecord
```

Expected store.search() signature after Plan 01:
```typescript
search(query: string, limit?: number, filter?: SearchFilter): SearchHit[]

interface SearchFilter {
  namespace?: string;
  agentId?: string;
  after?: string;   // ISO timestamp
  before?: string;  // ISO timestamp
}
```

Expected MemoryRecord after Plan 01:
```typescript
export interface MemoryRecord {
  id: string;
  title: string;
  content: string;
  metadata: Record<string, unknown>;
  namespace: string;
  agentId?: string;
  taskId?: string;
  expiresAt?: string;
  createdAt: string;
  updatedAt: string;
}
```

From packages/shared/src/constants.ts:
```typescript
export const MEMORY_NAMESPACES = {
  PUBLIC_CONCLUSIONS: 'public/conclusions',
  PUBLIC_BOARD: 'public/board',
  AGENT_PREFIX: 'agent',
} as const;
```

Current MemorySearchSchema (packages/shared/src/schemas.ts):
```typescript
export const MemorySearchSchema = z.object({
  query: z.string().min(1).max(1024),
  namespace: z.enum(['public', 'agent']).default('public'),
  limit: z.number().int().min(1).max(50).default(10),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update MCP server tools and shared schema to expose new store parameters</name>
  <files>packages/hive-memory/src/server.ts, packages/hive-memory/src/server.test.ts, packages/shared/src/schemas.ts</files>
  <read_first>
    - packages/hive-memory/src/server.ts
    - packages/hive-memory/src/store.ts
    - packages/shared/src/schemas.ts
  </read_first>
  <behavior>
    - Test: memory_add with {content, namespace, agentId, taskId, ttlMs} calls store.add with all fields and returns record
    - Test: memory_add with only {content} still works (backward compat)
    - Test: memory_search with {query, namespace} passes SearchFilter to store.search
    - Test: memory_search with {query, agentId, after, before} passes all filters to store.search
    - Test: memory_search with only {query} works as before (no filter object)
    - Test: memory_update unchanged, still works by id
  </behavior>
  <action>
    1. In packages/hive-memory/src/server.ts — update `memory_add` tool inputSchema:
       - Add `namespace: z.string().max(256).optional()`
       - Add `agentId: z.string().max(128).optional()`
       - Add `taskId: z.string().uuid().optional()`
       - Add `ttlMs: z.number().int().min(1000).optional()`
       - In the handler, pass all new fields to `store.add({title, content, metadata, namespace, agentId, taskId, ttlMs})`

    2. In server.ts — update `memory_search` tool inputSchema:
       - Add `namespace: z.string().max(256).optional()`
       - Add `agentId: z.string().max(128).optional()`
       - Add `after: z.string().datetime().optional()` — ISO timestamp lower bound
       - Add `before: z.string().datetime().optional()` — ISO timestamp upper bound
       - In handler, build filter: `const filter = { namespace, agentId, after, before }` (only include defined keys)
       - Call `store.search(query, resolvedLimit, filter)`

    3. `memory_update` — no changes needed, it works by id.

    4. In packages/shared/src/schemas.ts — update MemorySearchSchema for Gateway HTTP route:
       - Change namespace from `z.enum(['public', 'agent']).default('public')` to `z.string().max(256).optional()`
       - Add `agentId: z.string().max(128).optional()`
       - Add `after: z.string().datetime().optional()`
       - Add `before: z.string().datetime().optional()`

    5. Create packages/hive-memory/src/server.test.ts with tests using a real in-memory MemoryStore (`:memory:` db) and createMemoryMcpServer to verify the tools pass params correctly.
  </action>
  <verify>
    <automated>cd /Users/slr/hive && npx vitest run packages/hive-memory/src/server.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "namespace" packages/hive-memory/src/server.ts
    - grep -q "agentId" packages/hive-memory/src/server.ts
    - grep -q "taskId" packages/hive-memory/src/server.ts
    - grep -q "ttlMs" packages/hive-memory/src/server.ts
    - grep -q "after" packages/hive-memory/src/server.ts
    - grep -q "before" packages/hive-memory/src/server.ts
    - grep -q "agentId" packages/shared/src/schemas.ts
    - grep -q "after" packages/shared/src/schemas.ts
  </acceptance_criteria>
  <done>MCP tools accept and pass through namespace, agentId, taskId, ttlMs on add; namespace, agentId, after, before on search. Shared MemorySearchSchema updated. All backward compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Update Gateway MemoryService and search route to use new parameters</name>
  <files>packages/hive-gw/src/services/memory-service.ts, packages/hive-gw/src/routes/memory.ts, packages/hive-gw/src/services/memory-service.test.ts</files>
  <read_first>
    - packages/hive-gw/src/services/memory-service.ts
    - packages/hive-gw/src/routes/memory.ts
    - packages/hive-gw/src/services/memory-service.test.ts
    - packages/shared/src/schemas.ts
    - packages/shared/src/constants.ts
  </read_first>
  <action>
    1. In packages/hive-gw/src/services/memory-service.ts — update writeConclusion():
       - Add `namespace: MEMORY_NAMESPACES.PUBLIC_CONCLUSIONS` to the callTool args object
       - Add `agentId: task.assignee ?? 'unknown'` to the callTool args object
       - Add `taskId: task.id` to the callTool args object
       - Keep title as human-readable label (`${MEMORY_NAMESPACES.PUBLIC_CONCLUSIONS}/${task.id}`)

    2. Update writeProcess():
       - Add `namespace: \`${MEMORY_NAMESPACES.AGENT_PREFIX}/${agentId}\`` to callTool args
       - Add `agentId` and `taskId: task.id` to callTool args

    3. Update search() method:
       - Change signature from `search(query, namespace = 'public', limit = 10)` to `search(query: string, options?: { namespace?: string; agentId?: string; after?: string; before?: string; limit?: number })`
       - Remove the old scopedQuery prefix-mapping logic (`const scopedQuery = \`${pathPrefix} ${query}\``)
       - Instead, pass namespace directly to memory_search tool. For backward compat: if namespace is `'public'`, map to `MEMORY_NAMESPACES.PUBLIC_CONCLUSIONS`; if `'agent'`, map to `MEMORY_NAMESPACES.AGENT_PREFIX`. Otherwise pass as-is.
       - Pass all filter fields to the tool: `{ query, limit, namespace: resolvedNs, agentId, after, before }`

    4. In packages/hive-gw/src/routes/memory.ts — update GET /search:
       - Extract `agentId`, `after`, `before` from `req.query`
       - Include them in the MemorySearchSchema parse (the schema was already updated in Task 1)
       - Call `memoryService.search(query, { namespace, agentId, after, before, limit })`

    5. In packages/hive-gw/src/services/memory-service.test.ts — update tests:
       - Fix "public namespace" test: now should pass `namespace: "public/conclusions"` as a tool arg instead of prepending to query
       - Fix "agent namespace" test: now should pass `namespace: "agent"` as a tool arg
       - Add test: writeConclusion passes namespace, agentId, taskId as separate args to memory_add
       - Add test: search with agentId filter passes it to memory_search tool
       - Add test: search with after/before passes them to memory_search tool
  </action>
  <verify>
    <automated>cd /Users/slr/hive && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "namespace.*MEMORY_NAMESPACES" packages/hive-gw/src/services/memory-service.ts
    - grep -q "agentId" packages/hive-gw/src/services/memory-service.ts (in callTool args, not just conclusion object)
    - grep -q "taskId" packages/hive-gw/src/services/memory-service.ts (in callTool args)
    - grep -q "agentId" packages/hive-gw/src/routes/memory.ts
    - grep -q "after" packages/hive-gw/src/routes/memory.ts
    - grep -q "before" packages/hive-gw/src/routes/memory.ts
    - grep -q "writeConclusion.*namespace" packages/hive-gw/src/services/memory-service.test.ts
  </acceptance_criteria>
  <done>Gateway MemoryService passes namespace/agentId/taskId as dedicated fields on writes, search route accepts and forwards all filters, no more title-prefix namespace hack, all tests pass</done>
</task>

</tasks>

<verification>
- Full test suite passes: `npx vitest run` — all existing + new tests green
- MCP `memory_add` with `{content: "x", namespace: "ns", agentId: "a1"}` stores with namespace field
- MCP `memory_search` with `{query: "x", namespace: "ns"}` returns only "ns" entries
- Gateway writeConclusion passes namespace, agentId, taskId as dedicated tool args
- GET /memory/search?query=test&agentId=agent-1&after=2026-01-01T00:00:00Z returns filtered results
</verification>

<success_criteria>
1. memory_add MCP tool accepts and stores namespace, agentId, taskId, ttlMs
2. memory_search MCP tool accepts and applies namespace, agentId, after, before filters
3. Gateway MemoryService uses namespace field for writes and filtered search (no more prefix hack)
4. Gateway search route accepts agentId, after, before query params
5. All callers backward compatible — calls without new params work unchanged
6. Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-memory-enhancement/02-02-SUMMARY.md`
</output>
