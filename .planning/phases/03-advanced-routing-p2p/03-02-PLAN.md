---
phase: 03-advanced-routing-p2p
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/hive-gw/src/services/dispatcher.ts
  - packages/hive-gw/src/services/dispatcher.test.ts
  - packages/shared/src/types.ts
  - packages/shared/src/constants.ts
autonomous: true
requirements: [RT-03]

must_haves:
  truths:
    - "An agent idle for longer than starvation threshold gets priority boost"
    - "Starvation boost does not override capability requirement (still must be capable)"
    - "Recently-completed agents do not get starvation boost"
    - "Boost decays after agent receives a task"
  artifacts:
    - path: "packages/hive-gw/src/services/dispatcher.ts"
      provides: "Starvation guard logic in scoreAgent"
      contains: "starvation"
    - path: "packages/hive-gw/src/services/dispatcher.test.ts"
      provides: "Starvation prevention test cases"
      min_lines: 120
    - path: "packages/shared/src/constants.ts"
      provides: "STARVATION_THRESHOLD_MS, STARVATION_BOOST constants"
      contains: "STARVATION"
  key_links:
    - from: "packages/hive-gw/src/services/dispatcher.ts"
      to: "packages/shared/src/constants.ts"
      via: "import STARVATION constants"
      pattern: "STARVATION_THRESHOLD_MS|STARVATION_BOOST"
    - from: "packages/hive-gw/src/services/dispatcher.ts"
      to: "packages/hive-gw/src/services/task-machine.ts"
      via: "getAll() to compute last-completed time"
      pattern: "taskMachine\\.getAll"
---

<objective>
Add starvation prevention to the interest-first dispatcher so niche-interest agents still receive work.

Purpose: Without starvation guards, agents with uncommon interests could be perpetually skipped in favor of busy generalists. This ensures fairness while maintaining interest-first priority.
Output: Starvation boost integrated into scoring, with tests proving idle agents get priority uplift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/shared/src/types.ts
@packages/shared/src/constants.ts
@packages/hive-gw/src/services/dispatcher.ts
@packages/hive-gw/src/services/dispatcher.test.ts
@packages/hive-gw/src/services/registry.ts
@packages/hive-gw/src/services/task-machine.ts
@.planning/phases/03-advanced-routing-p2p/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan extends -->

From packages/shared/src/types.ts (after Plan 01):
```typescript
export interface RoutingScore {
  agent_id: string;
  interest: number;   // 0 or 50
  capability: number; // 0 or 20
  load: number;       // 0-30
  total: number;      // sum
}
```

From packages/shared/src/constants.ts (after Plan 01):
```typescript
export const ROUTING_WEIGHTS = {
  INTEREST_MATCH: 50,
  CAPABILITY_MATCH: 20,
  LOAD_BASE: 30,
  LOAD_PER_TASK: 10,
} as const;
```

From packages/hive-gw/src/services/dispatcher.ts (after Plan 01):
```typescript
export function scoreAgent(
  agent: RegisteredAgent,
  task: Task,
  loadMap: Map<string, number>
): RoutingScore;

export class Dispatcher {
  findBestAgent(requiredCapabilities: string[], task: Task): RegisteredAgent | null;
  autoAssign(task: Task): { task: Task; agent: RegisteredAgent } | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add starvation detection and scoring boost</name>
  <files>packages/shared/src/types.ts, packages/shared/src/constants.ts, packages/hive-gw/src/services/dispatcher.ts, packages/hive-gw/src/services/dispatcher.test.ts</files>
  <read_first>
    - packages/shared/src/types.ts
    - packages/shared/src/constants.ts
    - packages/hive-gw/src/services/dispatcher.ts
    - packages/hive-gw/src/services/dispatcher.test.ts
    - packages/hive-gw/src/services/task-machine.ts
  </read_first>
  <behavior>
    RED phase — add starvation tests to dispatcher.test.ts:
    - Test: agent idle > 60s gets +40 starvation boost to total score
    - Test: agent idle < 60s gets 0 starvation boost
    - Test: starvation boost can elevate a non-interest agent above an interest-matched agent (50 interest vs 40 starvation + 30 load = 70 > 50)
    - Test: agent with active task (working/claimed) gets 0 starvation boost regardless of idle time
    - Test: after agent receives assignment, its "last assigned" resets (no immediate re-boost)

    GREEN phase — implement starvation guard:
    - Track per-agent lastAssignedAt timestamp in Dispatcher (in-memory Map)
    - scoreAgent gains optional `lastAssignedAt` parameter
    - Starvation score: if (now - lastAssignedAt > STARVATION_THRESHOLD_MS) && agent has no active tasks: STARVATION_BOOST else 0
    - Update autoAssign to record lastAssignedAt when assignment happens
  </behavior>
  <action>
    1. Add to packages/shared/src/constants.ts (per RT-03):
       ```typescript
       export const STARVATION_THRESHOLD_MS = 60_000; // 60 seconds idle triggers boost
       export const STARVATION_BOOST = 40;            // enough to compete with interest match
       ```

    2. Extend RoutingScore in packages/shared/src/types.ts:
       ```typescript
       export interface RoutingScore {
         agent_id: string;
         interest: number;
         capability: number;
         load: number;
         starvation: number;  // NEW: 0 or STARVATION_BOOST
         total: number;
       }
       ```

    3. Write 5 new test cases in dispatcher.test.ts (TDD RED):
       - Create helper: `mockTimeAgo(ms)` returns ISO string ms ago
       - Test starvation boost presence when idle > threshold
       - Test no boost when idle < threshold
       - Test boost elevates niche agent over interest-matched busy agent
       - Test no boost when agent has active tasks
       - Test lastAssignedAt resets on autoAssign
       - Run tests, expect new tests FAIL

    4. Update scoreAgent in dispatcher.ts (TDD GREEN):
       - Add `lastAssignedAt?: string` and `hasActiveTasks?: boolean` params (or pass via options object)
       - Calculate starvation: `(!hasActiveTasks && lastAssignedAt && (Date.now() - new Date(lastAssignedAt).getTime() > STARVATION_THRESHOLD_MS)) ? STARVATION_BOOST : 0`
       - total = interest + capability + load + starvation
       - In Dispatcher class, add `private lastAssigned = new Map<string, string>()`
       - In findBestAgent: pass lastAssigned.get(agent.agent_id) to scoreAgent
       - In autoAssign: after successful claim, set lastAssigned.set(agent.agent_id, new Date().toISOString())
       - Run tests, expect ALL PASS

    5. Verify all previous Plan 01 tests still pass (no regression).
  </action>
  <verify>
    <automated>cd /Users/slr/hive && npx vitest run packages/hive-gw/src/services/dispatcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "starvation" packages/hive-gw/src/services/dispatcher.ts
    - grep -q "STARVATION_THRESHOLD_MS" packages/shared/src/constants.ts
    - grep -q "STARVATION_BOOST" packages/shared/src/constants.ts
    - grep -q "starvation" packages/shared/src/types.ts
    - grep -c "starvation" packages/hive-gw/src/services/dispatcher.test.ts returns >= 3
    - npx vitest run packages/hive-gw/src/services/dispatcher.test.ts exits 0
    - All Plan 01 tests still pass (no regression)
  </acceptance_criteria>
  <done>
    Starvation detection integrated into scoring formula. Idle agents (>60s with no active tasks) get +40 boost. Boost resets on assignment. 12+ total test cases pass including all Plan 01 tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update routing-score endpoint and verify end-to-end</name>
  <files>packages/hive-gw/src/routes/tasks.ts, scripts/smoke-test.sh</files>
  <read_first>
    - packages/hive-gw/src/routes/tasks.ts
    - packages/hive-gw/src/services/dispatcher.ts (after Task 1 changes)
    - scripts/smoke-test.sh
  </read_first>
  <action>
    1. Update GET `/tasks/:id/routing-score` in routes/tasks.ts:
       - Include starvation field in returned RoutingScore array
       - Add `lastAssignedAt` timestamp in response for observability

    2. Extend scripts/smoke-test.sh with interest-routing test:
       - Register agent "interest-agent" with capabilities=["research"], interests=["data-analysis"]
       - Register agent "generic-agent" with capabilities=["research"], interests=[]
       - Create task with requiredCapabilities=["research"], title containing "data-analysis"
       - Assert autoAssignedTo is "interest-agent" (interest match wins)
       - Create another task with requiredCapabilities=["research"], title="unrelated work"
       - Assert this second task also assigns (fallback works)
       - Add starvation scenario: wait briefly, create third task, verify assignment still works

    3. Run full smoke test: `bash scripts/smoke-test.sh`
  </action>
  <verify>
    <automated>cd /Users/slr/hive && npx vitest run && echo "--- unit tests passed ---"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "starvation" packages/hive-gw/src/routes/tasks.ts
    - grep -q "interest-agent" scripts/smoke-test.sh
    - grep -q "data-analysis" scripts/smoke-test.sh
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>
    Routing-score endpoint returns starvation field. Smoke test covers interest-first routing and fallback. All unit tests pass. End-to-end flow verified.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` passes all tests (Plan 01 + Plan 02)
- Starvation boost activates for idle agents
- Interest-first routing still works correctly (no regression)
- Smoke test covers interest routing scenario
- Routing-score diagnostic endpoint shows starvation field
</verification>

<success_criteria>
- Starvation prevention implemented with configurable threshold (RT-03)
- Idle agents get priority boost that can compete with interest match
- Boost resets on assignment (no permanent advantage)
- 12+ total unit tests pass
- Smoke test validates end-to-end interest routing
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced-routing-p2p/03-02-SUMMARY.md`
</output>
